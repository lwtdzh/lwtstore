name = "lwt-store"
compatibility_date = "2024-12-01"

# FILES KV - each key is a fileId, value is the file's full metadata
[[kv_namespaces]]
binding = "FILES"
id = "YOUR_FILES_KV_NAMESPACE_ID"
preview_id = "YOUR_FILES_PREVIEW_KV_NAMESPACE_ID"

# METADATA KV - reserved for future use (e.g., site config, user settings)
[[kv_namespaces]]
binding = "METADATA"
id = "YOUR_METADATA_KV_NAMESPACE_ID"
preview_id = "YOUR_METADATA_PREVIEW_KV_NAMESPACE_ID"

# GITHUB_PRIVATE_KEY should be set as a secret:
# wrangler secret put GITHUB_PRIVATE_KEY
